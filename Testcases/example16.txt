int add(int x, int y, int z) 
{
    return x + y + z;
}

int main() 
{
    int abacus;
    int babbage;
    int claude;
    int debian;
    abacus = 1;
    babbage = 2;
    claude = 3;
    debian = add(abacus, babbage, claude);
    return debian;
}
